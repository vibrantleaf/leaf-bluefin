# Purpose
i like bluefin i just wanted to change a couple of extra nice little things:3

rebase via rpm-ostree
```sh
rpm-ostree rebase ostree-image-signed:docker://ghcr.io/vibrantleaf/leaf-bluefin:latest
```
